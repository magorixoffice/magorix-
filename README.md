# Magorix

Next.js 14 (App Router), TypeScript, Tailwind CSS, `src/` layout, imports via `@/*`.

## Scripts

- `npm run dev` — start the dev server
- `npm run build` — production build
- `npm run start` — run the production server
- `npm run lint` — ESLint
